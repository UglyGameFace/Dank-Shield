from __future__ import annotations

"""Startup guard modules for Stoney Verify.

These are compatibility guards that still need to load before the main bot app,
but they live inside the application package instead of the repository root.
"""
